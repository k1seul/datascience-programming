/* Problem statement: problem.md */

/* Delete this define and every `NOT_IMPLEMENTED` below once you start writing.
 * The grader reads them as "not written yet"; anything else counts as a real answer. */
#define NOT_IMPLEMENTED (-1000)

#define MAX_VALUE 99

/* 1 when some value appears twice, 0 when not, -1 when a value is out of range. */
int has_duplicate(const int *values, int n)
{
    return NOT_IMPLEMENTED;
}

/* The value whose second occurrence comes earliest.
 * -1 when there is no duplicate, -2 when a value is out of range. */
int first_duplicate(const int *values, int n)
{
    return NOT_IMPLEMENTED;
}

/* Every value that appears at least twice, ascending, written to out.
 * Returns how many were written, -1 when out_capacity is too small,
 * -2 when a value is out of range. */
int find_duplicates(const int *values, int n, int *out, int out_capacity)
{
    return NOT_IMPLEMENTED;
}
