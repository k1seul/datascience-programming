// Problem statement: problem.md
//
// The grader calls this through ctypes, so the symbol must be exposed as extern "C".

// Delete this define together with the `return NOT_IMPLEMENTED;` below once you start.
// The grader reads it as "not written yet"; anything else counts as a real answer.
#define NOT_IMPLEMENTED (-1000)

// How many contiguous subarrays sum to k. Must run in O(n).
extern "C" int subarray_sum(const int *values, int n, int k)
{
    return NOT_IMPLEMENTED;
}
