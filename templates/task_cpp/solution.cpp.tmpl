// Problem statement: problem.md
//
// The grader calls this through ctypes, so the symbol must be exposed as extern "C".

// (one-line specification) Returns the number of items written to out, or -1 on error.
extern "C" int solve(const int *values, int n, int *out, int out_capacity)
{
    return -1;  // TODO
}
