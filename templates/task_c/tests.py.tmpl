"""Week {{week}} / C ({{difficulty_label}}) 채점 테스트."""

from __future__ import annotations

from pathlib import Path

import pytest

from harness import list_out_call, native_lib

HERE = Path(__file__).resolve().parent
SRC = HERE / "solution.c"


@pytest.fixture
def solve():
    lib = native_lib(SRC)

    def call(values: list[int], capacity: int | None = None) -> list[int]:
        return list_out_call(lib.solve, values, capacity=capacity)

    return call


@pytest.mark.parametrize(("given", "expected"), [])
def test_기본_케이스(solve, given, expected):
    assert solve(given) == expected
